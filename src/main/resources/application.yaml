spring:
   datasource:
     username: pet_parks
     password: pet_parks
     url: jdbc:mysql://localhost:3306/pet_parks
     
   jps:
     Hibernate:
        ddl-auto: create
     show-sql: true
     defer-datasource-initialization: true
     
   sql:
     init:
       mode: always

   
          